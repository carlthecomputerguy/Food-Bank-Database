[phases.setup]
nixPkgs = ["python310Full"]

[phases.install]
cmds = ["python3 -m ensurepip --default-pip", "python3 -m pip install --upgrade pip", "python3 -m pip install -r requirements.txt"]

[start]
cmd = "gunicorn food_bank_server_cloud:app --bind 0.0.0.0:$PORT"
